spring:
  application:
    name: tourism-agency

  datasource:
    url: jdbc:mysql://database:3306/tourism-db?autoReconnect=true&connectTimeout=10000&socketTimeout=30000&retriesAllDown=3
    username: admin-mau
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
      show-sql: true
      properties:
        hibernate.format_sql: true




